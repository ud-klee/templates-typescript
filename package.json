{
  "name": "templates-typescript",
  "version": "0.1.0",
  "description": "",
  "private": true,
  "scripts": {
    "build": "tsc",
    "lint": "prettier -c '**/*.{ts,tsx}' && eslint --cache --max-warnings=0 '**/*.{ts,tsx}'",
    "lint:fix": "prettier --write '**/*.{ts,tsx}' && eslint --cache --fix '**/*.{ts,tsx}'",
    "prepare": "husky install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "lint-staged": {
    "*.{ts,tsx}": [
      "prettier --write",
      "eslint --cache --fix"
    ]
  },
  "devDependencies": {
    "@types/node": "^18.13.0",
    "@typescript-eslint/eslint-plugin": "^5.51.0",
    "@typescript-eslint/parser": "^5.51.0",
    "eslint": "^8.33.0",
    "husky": "^8.0.3",
    "lint-staged": "^13.1.2",
    "prettier": "^2.8.4",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5"
  }
}
